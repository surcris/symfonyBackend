{% extends 'base.html.twig' %}

{% block title %}Hello ContactController!{% endblock %}

{% block body %}

    {{ form_start(form) }}
        {{ form_rest(form) }} 
    {{ form_end(form) }}
    {% if msg is defined %}
        <p>{{ msg }}</p>
    {% endif %}
    {% if status is defined %}
        <p>{{ status }}</p>
    {% endif %}
{% endblock %}
